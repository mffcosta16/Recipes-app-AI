package com.recipes.domain.shared;

/**
 * Marker for domain value objects: immutable, defined by their attribute values rather than
 * an identity, and always valid once constructed.
 */
public interface ValueObject {

}
